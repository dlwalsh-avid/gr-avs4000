This is the avs4000-write-a-block package meant as a guide to building
out-of-tree packages. To use the avs4000 blocks, the Python namespaces
is in 'avs4000', which is imported as:

    import avs4000

See the Doxygen documentation for details about the blocks available
in this package. A quick listing of the details can be found in Python
after importing by using:

    help(avs4000)
